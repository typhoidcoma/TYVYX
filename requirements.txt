# Runtime requirements for TYVYX drone controllers and web app
opencv-python>=4.8.0
numpy>=1.24.0
flask>=2.0.0

# Autonomous system dependencies (Phase 2+)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
pyyaml>=6.0.1
python-socketio>=5.10.0
aiofiles>=23.2.1

# Optional (heavy) dependency for YOLO integration - enable only if needed
# ultralytics>=8.0.0

# Helper for advanced network inspection (optional but recommended)
psutil>=5.9.0

# NOTE: FFmpeg is required on the system PATH for reliable RTSP playback
# (Not a Python package). Verify with `ffmpeg -version`.

# Optional: packet capture helper. Install to enable `tyvyx.tools.packet_sniffer`
# On Windows, also install Npcap (https://nmap.org/npcap/) and run as admin.
scapy>=2.4.5